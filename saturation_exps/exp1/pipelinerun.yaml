apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: saturation-exp1-20260518-201146
spec:
  timeouts:
    pipeline: 6h
    tasks: 5h30m
  taskRunTemplate:
    serviceAccountName: helm-installer
  pipelineRef:
    name: blis-1-llama-3-1-8b-instruct
  workspaces:
    - name: model-cache
      persistentVolumeClaim:
        claimName: model-pvc
    - name: data
      persistentVolumeClaim:
        claimName: data-pvc
    - name: hf-credentials
      secret:
        secretName: hf-secret
        items:
          - key: HF_TOKEN
            path: HF_TOKEN
    - name: target-credentials
      secret:
        secretName: s3-secret
        items:
          - key: ACCESS_KEY
            path: ACCESS_KEY
          - key: SECRET_KEY
            path: SECRET_KEY
